import { FC } from 'react'
import {Compilations} from "../features/compilations/compilations";

export const CompilationsPage: FC = () => {
  return (
    <div className="page">
      <Compilations />
    </div>
  )
}
